name: Ubuntu 18.04 LTS Debug x64

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-18.04

    steps:
      - uses: actions/checkout@v2
      - name: install ubuntu dependencies
        run: sudo ./.vscode/install_ubuntu_dependencies.sh
      - name: build ubuntu debug x64
        run: ./.vscode/build.sh
      - name: test ubuntu debug
        run: ./.vscode/test.sh
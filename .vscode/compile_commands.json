[
    {
        "directory": "${workspaceFolder}/src",
        "command": "clang++ $(llvm-config --cxxflags --ldflags --system-libs --libs all) ${workspaceFolder}/src/mc.cpp -o ${workspaceFolder}/mc.o",
        "file": "${workspaceFolder}/src/mc.cpp"
    }
]